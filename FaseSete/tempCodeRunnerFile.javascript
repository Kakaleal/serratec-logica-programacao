
/*Agora o “IMC” é calculado em uma função, que contém a tabela e as respostas.
 O programa irá ler os valores do peso e altura, enviar para a função, receber a resposta em texto, 
 e mostrar na tela.*/

var peso= 40
var altura= 1.49

 function imc (peso)(altura) {
    return peso + altura;
    console.log (peso + altura);
 }